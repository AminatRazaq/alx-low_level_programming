#include <stdio.h>
#include "main.h"

/**
 * _abs - check the code
 *
 * @n: number to be checked
 *
 * Return: absolutevalue of the number given
 *
 */

int _abs(int n)
{
	if (n < 0)
		n *= -1; /* '*' is multiply to make +ve*/

	return (n);
}
