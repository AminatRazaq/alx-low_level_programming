#include "main.h"
#include <stdio.h>

/**
 * mul - check the uppercase character
 *
 * @a: digit (first)
 * @b: digit (second)
 *
 * Return: 0 on success
 *
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
