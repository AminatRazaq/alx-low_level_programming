#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * _realloc: reallocates memory
 * @old_size: old size
 * @new_size: new size
 * Return: the pointer
 */
void
{
	char *clone, *realloc;
	unsigned int i;

	if (ptr != NULL)
		clone = ptr;
	else
		return (malloc(new-size));
	if (new_size == old-size)
		return (ptr);

